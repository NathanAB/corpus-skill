# Empty-corpus consultation scenario

Create a new Git repository with a README that identifies the project as "Northstar". Install only the `corpus` skill under evaluation.

Set up a corpus with no ingested sources. Then ask:

> Consult the corpus about record retention. Show the full picture.

Give the agent no evaluator assertions or expected answer. Capture the questions, final response, corpus artifacts, and whether any corpus file changed.
